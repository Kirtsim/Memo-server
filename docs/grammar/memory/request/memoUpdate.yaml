POST /memo-update
{
  "id": "memory-id",
  "data": {
     "title": "Title",
     "description": "long string containing a description of the memory",
     "addTags": ["tag-name1", "tag-name4", ...],
     "removeTags": ["tag-name2", "tag-name3", ...]
  }
}
